import { TittleContainer, TittleText } from "./Title.styled";

export const Title = () => {
  return (
   <TittleContainer>
    <TittleText>Medicine Store</TittleText>
    </TittleContainer>
      
    
  );
}